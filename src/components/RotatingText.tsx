'use client';

import ReactRotatingText from 'react-rotating-text';

const items = ['능동적으로 일하는', '꾸준히 공부하는'];

export default function RotatingText() {
  return <ReactRotatingText items={items} />;
}
